apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: solar-system
  name: solar-system
  namespace: solar-system
spec:
  selector:
    matchLabels:
      app: solar-system
  template:
    metadata:
      labels:
        app: solar-system
    spec:
      # imagePullSecrets:
      # - name: my-ecr-registry-key
      containers:
      - name: solar-system
        image: chinmayapradhan/orbit-engine:e7a7eec18b2ab36656bd2783b73880c2c3b35686
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http 
          protocol: TCP 
        envFrom:
        - secretRef:
            name: mongo-db-creds

